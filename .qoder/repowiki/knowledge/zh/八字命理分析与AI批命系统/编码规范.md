- 自定义 Hooks 模式：业务逻辑（如八字计算、AI 请求）被封装在 `src/hooks/` 下的自定义 Hook 中，实现逻辑复用与 UI 解耦。
- 类型驱动开发：在 `src/types/` 中集中定义严格的 TypeScript 接口（如 `Ganzhi`, `FourPillars`），并在工具函数与组件间共享以确保类型安全。
- 样式模块化：每个功能组件（如 `InputForm`, `BaziChart`）均配有同名的 `.css` 文件，实现样式的局部作用域管理。